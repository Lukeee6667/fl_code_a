# 第二阶段：基于辅助数据的自适应掩码净化 (Config 18)

## 1. 核心目标
针对在第一阶段（基于统计指标的检测与过滤）未能完全过滤的隐蔽后门（例如分布极度接近良性的非独立同分布攻击），本阶段采用**主动防御**策略。利用少量良性辅助数据，通过对抗博弈（Min-Max Game）主动搜索并物理切除后门神经元，实现对模型的深度净化。

## 2. 完整处理流程

整个 Config 18 的处理流程包含三个关键步骤，其中**步骤 3（轻量化微调）为本方案的核心创新点**。

### 步骤 1：掩码初始化 (Mask Initialization)
*   **输入**：第一阶段生成的聚合模型。
*   **过程**：在辅助数据上初始化并预优化模型掩码（Mask）。
*   **目的**：寻找一个初始状态，使得掩码在保持正常分类准确率的同时，尽可能保留模型的大部分良性功能（即掩码值尽可能接近 1）。这为后续的对抗搜索提供了一个良好的良性基准。

### 步骤 2：双层对抗优化 (Dual Adversarial Optimization)
系统进入核心的交替优化循环（Min-Max Game），通过博弈来定位后门神经元：

*   **内层循环（模拟攻击者）**：
    *   固定模型掩码，寻找最强的**对抗性扰动**。
    *   这相当于在辅助数据上主动“合成”一个虚拟的后门触发器，使得模型在当前掩码下产生错误分类。这一步模拟了攻击者可能植入的最坏情况。
*   **外层循环（防御者）**：
    *   固定对抗扰动，优化**模型掩码**。
    *   目标是找到一个最优的子网络结构，使得即便输入加上了上述最强扰动，模型依然能给出正确的预测。
*   **物理切除**：
    *   经过多轮博弈，掩码成功“锁定”了所有对后门敏感的神经元（即那些被切断后模型就不再受扰动影响的路径）。
    *   系统将此掩码应用到全局模型上，对这些风险神经元进行**物理切除（Pruning）**，阻断后门通路。

---

### 步骤 3：【创新点】轻量化微调与交互约束 (Lightweight Fine-tuning with Interaction Constraints)

这是本方案区别于传统剪枝防御（如 Pruning-only 方法）的关键创新所在。

#### **3.1 创新背景**
传统的剪枝防御往往面临一个两难困境（Trade-off）：
*   **剪枝过度**：虽然彻底清除了后门，但也误伤了良性神经元，导致模型在正常数据上的准确率（Clean ACC）大幅下降。
*   **直接微调的风险**：如果剪枝后直接对模型进行普通微调，虽然能恢复准确率，但残存的后门隐患可能会在参数更新过程中“死灰复燃”（Re-activation），导致防御失效。

#### **3.2 创新机制：带交互约束的微调 (Interaction-Constrained Fine-tuning)**
为了解决上述问题，我们引入了带有**交互约束**的微调机制：

1.  **数据驱动的性能恢复**：
    *   利用极少量的良性辅助数据，对经过剪枝后的模型进行短周期的轻量化微调。
    *   目的：快速修复因结构化剪枝造成的模型表达能力损伤，重建良性分类边界。

2.  **严格的梯度交互约束 (Gradient Interaction Constraint)**：
    *   在微调的反向传播过程中，我们利用步骤 2 生成的最终掩码（Mask）作为**梯度过滤器**。
    *   **核心逻辑**：对于那些已经被判定为“后门相关”并被剪枝的神经元（Mask = 0），我们在微调时强制将其梯度置为 0。
    *   **代码实现逻辑**：
        ```python
        if module.weight.grad is not None:
            # 仅允许良性神经元更新，彻底锁死后门神经元
            module.weight.grad.data.mul_(mask)
        ```

#### **3.3 创新价值**
*   **安全性与可用性的双重保障**：这种机制确保了模型只能在“良性子空间”内进行优化。既能像普通微调一样恢复良性准确率，又像“锁”一样严防后门路径的重激活。
*   **低成本高效益**：仅需少量辅助数据和数轮迭代，即可显著提升最终模型的综合表现（High Clean ACC + Low ASR），使其真正具备落地使用的价值。
