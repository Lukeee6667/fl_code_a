# A4FL 防御流程 (Config 12)

## 1. 核心目标
针对联邦学习中的复杂攻击（如隐蔽后门、Sybil 攻击等），本配置采用了一种**多指标联合检测与聚类筛选**的防御策略。旨在通过多维度的特征分析，精准识别并剔除恶意客户端，同时保留良性客户端的贡献。

## 2. 方法概述
本方案结合了现有的方向性检测指标与**创新引入的幅度/一致性指标及聚类算法**，构建了一个三层过滤体系。

### 2.1 现有基础 (Existing Methods)
这部分沿用了 AlignIns 的核心思想，主要关注更新向量的方向一致性：
*   **TDA (Total Direction Alignment)**:
    *   计算客户端更新与全局模型更新（或平均更新）之间的余弦相似度。
    *   *作用*：检测方向上显著偏离主群体的恶意更新。
*   **MPSA (Masked Parameter Sign Alignment)**:
    *   在稀疏化的参数子空间内，比较客户端更新符号与主导符号的一致性。
    *   *作用*：在细粒度参数层面捕捉异常行为。

### 2.2 核心创新 (Innovations)
为了弥补仅依赖方向检测的不足，本方案引入了以下关键创新点：

#### **A. 新增检测指标**
1.  **梯度范数 (Gradient Norm)**:
    *   *定义*：计算客户端更新向量的 $L_2$ 范数。
    *   *创新点*：许多攻击（如缩放攻击、特定后门）会导致更新幅度的异常放大或缩小。引入 Grad Norm 可以有效捕捉这种**幅度上的异常**，填补了 TDA/MPSA 仅关注方向/符号的盲区。
2.  **平均余弦相似度 (Mean Cosine Similarity)**:
    *   *定义*：计算某客户端更新与其他所有客户端更新的余弦相似度的均值。
    *   *创新点*：不同于 TDA 仅与全局参照对比，Mean Cos 衡量了**客户端之间的群体一致性**。恶意客户端往往在群体中显得“格格不入”，该指标能更敏感地反映这种离群特性。

#### **B. 聚类筛选机制 (Clustering-based Filtering)**
*   **传统方法的局限**：传统方法通常设定硬阈值（如 MZ-score > 阈值）来切断异常。这在非独立同分布（Non-IID）数据下容易误伤偏离中心的良性客户端。
*   **聚类创新**：
    *   **特征构建**：将上述四个指标（TDA, MPSA, Grad Norm, Mean Cos）的 MZ-score 组合成一个 4 维特征向量。
    *   **K-Means 聚类**：利用 K-Means 算法在 4 维特征空间中将客户端自动划分为两个簇（良性簇 vs 恶意簇）。
    *   **优势**：利用高维空间的信息互补性，实现**自适应的动态分类**，而非依赖死板的阈值。即使恶意攻击在某一两个指标上伪装得很好，也很难在所有四个指标的联合分布上与良性群体完全混淆。

## 3. 完整处理流程

1.  **指标计算 (Metric Calculation)**:
    *   服务器接收所有客户端的更新。
    *   并行计算 TDA, MPSA, Grad Norm, Mean Cos 四个指标。
2.  **标准化 (Normalization)**:
    *   对每个指标计算 MZ-score (Modified Z-score)，消除量纲差异，突出离群程度。
3.  **聚类判别 (Clustering)**:
    *   输入：所有客户端的 4 维 MZ-score 向量。
    *   执行 K-Means (k=2) 聚类。
    *   判定：根据簇的大小（假设恶意者占少数）或簇中心特征（良性簇通常具有更低的异常得分），确定良性簇。
4.  **聚合 (Aggregation)**:
    *   仅保留被判定为“良性”簇内的客户端更新。
    *   执行加权平均（FedAvg）生成新的全局模型。
